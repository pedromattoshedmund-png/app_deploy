name: CD Básico K3s

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verificar cluster
        run: sudo kubectl get nodes

      - name: Deploy no K3s
        run: |
          sudo kubectl apply -f k8s/
          sudo kubectl rollout status deployment/demo-app
